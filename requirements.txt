pandas>=2.0
numpy>=1.24
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.3
matplotlib>=3.7
streamlit>=1.30
altair>=5.0

# Test-time only; `make setup`, `make pipeline` and `make dashboard` do not need it.
pytest>=7.0
